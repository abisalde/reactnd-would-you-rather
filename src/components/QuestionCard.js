import React, { Fragment } from 'react';
import { Card } from 'react-bootstrap';

const QuestionCard = () => {
    return (
        <Fragment>
            <Card></Card>
        </Fragment>
    );
};

export default QuestionCard;
